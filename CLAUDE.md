# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクトの目的

日本政府が公式・準公式に提供する MCP サーバーへ接続し、

1. 接続と利用の検証（どのツールで何が取れるか）
2. 取得結果の**データ台帳**化（出典・取得条件・項目・鮮度を記録した一次資産）
3. 台帳から生成した**アウトプットで何が知られるか**の検証（カバレッジ・欠損・粒度の限界を明文化）

を行う。最終形は「ひとつのアウトプットとその定期運用を確立する」こと。網羅的なデータ収集基盤ではなく、
特定業種・テーマに絞った検証プロジェクトである点を常に優先する。

## 現状

コードはまだ無い（`README.md` のみ）。ビルド・テスト・lint コマンドは未定義。
最初の実装が入った時点で、このファイルにコマンド節とアーキテクチャ節を追記すること。
**存在しないコマンドをこのファイルに書かない。**

## 接続先 MCP の前提（調査済みの事実）

「政府公式」と「政府APIを叩く第三者製ラッパー」は性質がまったく違う。台帳・レポートでは必ず区別して記録する。

| 対象 | 提供元 | 種別 | APIキー | 備考 |
|---|---|---|---|---|
| [jgrants-mcp-server](https://github.com/digital-go-jp/jgrants-mcp-server) | デジタル庁 | **公式** | 不要 | 補助金（Jグランツ）。MIT / Python 3.11+ / Streamable-HTTP |
| [mlit-dpf-mcp](https://github.com/MLIT-DATA-PLATFORM/mlit-dpf-mcp) | 国土交通省 | **公式** | 要（要アカウント登録＋アプリ登録） | 国土交通データプラットフォーム。裏側は GraphQL |
| [e-Stat API](https://www.e-stat.go.jp/api/) | 総務省統計局 | 公式API（公式MCPは無し） | 要（appId 登録） | 政府統計。MCP 経由なら第三者ラッパー |
| e-Gov 法令API | デジタル庁 | 公式API（公式MCPは無し） | 不要 | 法令本文。MCP は第三者実装が複数 |

Jグランツ公式サーバーの提供ツール: `search_subsidies` / `get_subsidy_detail` / `get_subsidy_overview` /
`get_file_content` / `ping`。キーワード・業種・地域・従業員数での絞り込みと、添付 PDF 等の Markdown 変換に対応。
起動は `python -m jgrants_mcp_server.core --host 0.0.0.0 --port 8000`、MCP エンドポイントは `http://localhost:8000/mcp`。
（このリポジトリ内には未導入。導入時は本節を実測値で更新すること。）

## データ台帳の扱い

台帳はこのプロジェクトの成果物本体であり、スクリプトの副産物ではない。行を追加・更新するときは必ず次を伴わせる。

- **取得日時**（政府側データは随時更新され、再現には時点が要る）
- **取得元**（サーバー名／ツール名）と**リクエストパラメータ一式**（同じ結果を再取得できる形で）
- **一次情報URL**（対応する政府サイト側のページ）

政府データを加工した数値を出力に載せるときは、加工前の生データを台帳側に残す。
取得できなかった・欠損した項目は空欄にせず「取れなかった」と記録する — 検証の主目的が
「アウトプットから何が知られるか（＝何が知られないか）」の把握にあるため、欠損そのものが結果になる。

## リポジトリ運用

- 作業ブランチ: `claude/japan-gov-mcp-project-1to4zi`（`main` へ直接 push しない）
- 取得した生データや大きなスナップショットをコミットする際は、対象業種・時点が分かるパス設計にする
