# gov-data

日本政府公式・および信頼性の高いデータ提供サービスの**情報源台帳**。
台帳の全エントリが、再実行可能な検証スクリプトと対になっている。

作業の手順そのものはここではなく `.claude/skills/gov-data-registry/SKILL.md` にある。
ここに書くのは、どんな作業をしていても常に効く制約だけ。

## このリポジトリで最も守るべきこと

台帳は放っておくと腐り、**腐ったことに誰も気づけない**。
書かれていることが実測と食い違った瞬間、この台帳は無いより悪くなる。
以下はすべてその一点から出ている。

## 常に効く制約

### 生成物を手で編集しない

`docs/REGISTRY.md`、`docs/FIELDS.md`、`registry/registry.json` は
`registry/build.py` の生成物。手で直しても次のビルドで失われるうえ、
失われるまでのあいだ台帳が実測と違う内容を主張する。
内容を変えるときは `registry/sources/*.yaml` か `results/` を直してビルドし直す。

同じ理由で、**生成物の内容を他の文書にコピーしない**。README に台帳の一覧表を
再掲しない。二重管理は必ずずれる。

### 推測を書かない

- 実行していない手順を書かない。動くはずの手順ではなく、動いた手順だけを書く
- 一次資料に無い数値や制約を、経験や一般論から補わない
- 一次資料に到達できないまま、Web 検索や解説記事の内容でエントリを埋めない
- 到達できなかったことを「提供終了」と解釈しない
- 一次資料に記述が無いことは `undocumented`。「なし」や空欄にしない

### 検証とビルドは `.work/toolvenv/bin/python` で動かす

システムの `python` には httpx も PyYAML も入っていない。
この venv はセッション開始時のフックが用意する。無ければ
`./verify/run_all.sh` を一度回す。

### ファイル編集は結果を確かめる

文字列置換で目的の箇所にマッチしなかった場合、多くのツールは黙って何もしない。
このリポジトリでは実際にそれが起き、README から原則が 1 つ抜け落ちたまま
気づかれずに残った。置換したら必ず、置換後の内容を読んで確かめる。
スクリプトで置換するなら `assert` を入れる。

### 報告は実測に基づく

「通った」「動いた」と書くのは、実際に実行して確かめたときだけ。
`registry/build.py` が通ったことは「検証済み」を意味しない
（未検証のエントリがあってもビルドは通る）。エントリの状態は出力で確認する。

## セッションの開始と終了

**このリポジトリのコンテナは毎回作り直される。コミットしていないものは失われる。**
前回の経緯も、書き残さなければ次のセッションには何も残らない。

- **開始時**: 前回の引き継ぎ（`.claude/diary/` の最新）はフックが自動で読み込む。
  ただしそれは書かれた時点のスナップショットであって現在の状態ではない。
  着手前に `git log` と `git status` で実測して確かめる。
  遡って読むときや読み直すときは `/activate`
- **終了時**: `/handoff` で引き継ぎを書き、**コミットして push する**。
  push まで終えて初めて引き継ぎが成立する

引き継ぎには、うまくいかなかったことと判断の理由を必ず書く。
成功だけを書いた引き継ぎは、次のセッションに同じ壁を踏ませる。

## 作業を終える前に

台帳まわり（`registry/`、`verify/`、`results/`、`docs/`）を触ったら:

```bash
.work/toolvenv/bin/python registry/build.py   # 生成物を作り直す
```

`registry/build.py` の判定ロジックを触ったら、加えて:

```bash
bash registry/test_build.sh                   # 検査が効いているかの回帰テスト
```

情報源を足したり検証スクリプトを変えたら、通しで:

```bash
./verify/run_all.sh                           # 全検証 + 項目抽出 + 台帳再生成
```

## 機械で止めていること

以下は注意力ではなくフック（`.claude/settings.json`）とビルドの検査で強制している。
迂回しようとせず、止められたら原因を直す。

| 仕組み | 何を止めるか |
|---|---|
| PreToolUse フック | 生成物への手編集 |
| Stop フック | 生成物が古いままターンを終えること |
| PostToolUse フック | `build.py` の検査を壊したまま進むこと |
| SessionStart フック | venv が無くてコマンドが動かない状態 / 前回の引き継ぎの読み飛ばし |
| Stop フック（催促） | 引き継ぎを残さずセッションが途切れること（セッション中 1 回だけ通知。ブロックはしない） |
| `build.py` の検査 | スキーマ違反、`status` の手書き、`id` 不一致、`run_all.sh` からの実行漏れ、結果の `source_id` 不一致、`measured` の数値と実測の食い違い |
